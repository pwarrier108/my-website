---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const category = 'sustainable-ai';
const base = import.meta.env.BASE_URL;

const workContent = await getCollection('work', ({ data }) => 
    data.category === category && (data.status === 'published' || data.status === 'canonical')
);

const sortedContent = workContent.sort((a, b) => 
    b.data.date.valueOf() - a.data.date.valueOf()
);
---

<BaseLayout title="Sustainable AI Era (2020s) — Work" currentPage="work">
    <div class="page-container">
        <h1>Sustainable AI Era (2020s)</h1>
        
        <p>
            Climate-resilient AI, sustainable technology innovation, NITK AI Centre of Excellence, 
            and regenerative systems design. Building AI that serves humanity and the planet 
            without compromising future generations.
        </p>

        {sortedContent.length > 0 ? (
            <ul class="content-list">
                {sortedContent.map((item) => (
                    <li class="content-item">
                        <h3>
                            <a href={`${base}/work/sustainable-ai/${item.slug}`}>
                                {item.data.title}
                            </a>
                        </h3>
                        {item.data.summary && <p class="summary">{item.data.summary}</p>}
                        <div class="metadata">
                            <span class="date">{item.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long' })}</span>
                            {item.data.era && <span class="era">· Era: {item.data.era}</span>}
                        </div>
                    </li>
                ))}
            </ul>
        ) : (
            <p><em>Content coming soon.</em></p>
        )}
    </div>
</BaseLayout>
